spring:
  datasource:
    url: jdbc:mariadb://localhost:3305/customer_management
    username: root
    password: 123
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
